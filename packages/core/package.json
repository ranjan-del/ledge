{
  "name": "@ledge/core",
  "version": "0.1.0",
  "description": "Shared logic for Ledge: task files, config, store, git scan and prompt building.",
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./pure": "./src/pure.ts"
  },
  "engines": { "node": ">=22.6" },
  "scripts": {
    "test": "node --test test/*.test.ts",
    "typecheck": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "yaml": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "^5.6.0"
  }
}
